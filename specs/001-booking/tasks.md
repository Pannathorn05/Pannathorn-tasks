# Tasks: จองคิวตรวจสุขภาพ (Booking)
Feature: จองคิวตรวจสุขภาพ (Booking) | Spec ID: SPEC-BKG-001 (Draft v2) | อ้างอิง: plan.md (plan v1) | วันที่: 2569-09-23

> มีทั้งหมด 21 task แบ่งเป็นพร้อมทำ 14 task, รอ Q-02 1 task (T-12) และรอทีมตัดสินใจ 6 task (T-06, T-07, T-10, T-15, T-20, T-21)
> task ที่ "รอทีมตัดสินใจ" คือ task ที่ spec หรือ plan ยังไม่ชัด แต่ยังไม่มี Q ID ให้อ้าง (AI สร้าง ID ใหม่เองไม่ได้) รายละเอียดอยู่ในหัวข้อ "ข้อสังเกตที่ทีมต้องตัดสินใจ" ท้ายไฟล์

สถานะที่ใช้ในไฟล์นี้: `พร้อมทำ` | `รอ Q-xx` | `รอทีมตัดสินใจ (ข้อสังเกต N)`

---

## หลังบ้าน: พื้นฐานและข้อมูล

### T-01 ตั้งโครง FastAPI app, config และ session ฐานข้อมูล
- รองรับ: CON-TECH-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-02
- ไฟล์ที่แตะ: `backend/app/main.py`, `backend/app/config.py`, `backend/app/db/session.py`, `backend/tests/conftest.py`, `backend/tests/test_T01_setup.py`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: `cd backend && pytest` รันได้โดย conftest สร้าง session SQLite ในหน่วยความจำ และ app อ่าน `DATABASE_URL` จาก config ได้
- สถานะ: เสร็จ

### T-02 สร้างตาราง slots, bookings, audit_logs และ migration 001_init
- รองรับ: CON-TECH-01, DOM-PDPA-01, IF-HIS-01, FR-BKG-04
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-04, T-05, T-13
- ไฟล์ที่แตะ: `backend/app/db/models.py`, `backend/app/db/migrations/001_init.py`, `backend/tests/test_IF_HIS_01_schema.py`
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: test ยืนยันว่า `upgrade(engine)` สร้างครบ 3 ตารางตาม plan ข้อ 3 และตาราง bookings ไม่มีคอลัมน์เลขบัตรประชาชน (`queue_no` ว่างได้)
- สถานะ: เสร็จ

### T-03 ตรวจผลยืนยันตัวตนก่อนเข้าถึงทุก endpoint
- รองรับ: IF-IDP-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-04, T-05, T-11, T-13, T-14 (เงื่อนไข Given "ยืนยันตัวตนแล้ว" ของ AC-BKG-01)
- ไฟล์ที่แตะ: `backend/app/auth/idp.py`, `backend/tests/conftest.py`, `backend/tests/test_IF_IDP_01.py`
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: test ยืนยันว่า request ที่ไม่มีผลยืนยันตัวตนถูกปฏิเสธ ส่วน request ที่มีผลยืนยันตัวตนผ่าน (ใช้ตัวตรวจจำลองใน test เพราะ spec และ plan ยังไม่ระบุวิธีเชื่อมระบบยืนยันตัวตนจริง ดูข้อสังเกต 5)
- สถานะ: เสร็จ

## หลังบ้าน: ช่วงเวลาและการจอง

### T-04 สร้าง GET /slots แสดงช่วงว่าง 30 วันตามแพ็กเกจ
- รองรับ: FR-BKG-01, FR-BKG-06 (อ่านโควตาตาม ASM-01 และใช้เขตเวลา Asia/Bangkok ตาม ASM-02)
- ตรวจด้วย: ไม่มี AC ตรง ๆ (FR-BKG-01 ถูกตรวจความเร็วใน AC-BKG-05 ที่ T-16 ส่วน FR-BKG-06 ยังไม่มี AC ดูข้อสังเกต 6)
- ไฟล์ที่แตะ: `backend/app/slots/router.py`, `backend/app/slots/service.py`, `backend/app/main.py`, `backend/tests/test_FR_BKG_01_06_slots.py`
- ต้องทำหลัง: T-02, T-03
- เสร็จเมื่อ: test ยืนยันว่า GET /slots คืนเฉพาะช่วงภายใน 30 วันข้างหน้าพร้อม remaining และเมื่อเปลี่ยน `package_code` ได้ช่วงของแพ็กเกจนั้น
- สถานะ: เสร็จ รอทีมตรวจ

### T-05 สร้าง POST /bookings: บันทึกการจองและตัดที่นั่ง
- รองรับ: FR-BKG-04
- ตรวจด้วย: AC-BKG-01 (ส่วน "บันทึกสำเร็จ" และ "ที่นั่งว่างเป็น 0" ส่วน "แสดงหมายเลขคิว" อยู่ที่ T-12 และ T-19)
- ไฟล์ที่แตะ: `backend/app/booking/router.py`, `backend/app/booking/service.py`, `backend/app/main.py`, `backend/tests/test_AC_BKG_01.py`
- ต้องทำหลัง: T-02, T-03
- เสร็จเมื่อ: `test_AC_BKG_01` ผ่าน: ช่วง 09.00 ที่เหลือ 1 ที่ถูกจองสำเร็จ และ remaining เป็น 0 (ตัดที่นั่งแบบมีเงื่อนไข remaining > 0 ในคำสั่งเดียว เพื่อให้ T-07 กันการจองซ้อนได้)
- สถานะ: พร้อมทำ

### T-06 ปฏิเสธการจองซ้ำในวันเดียวกันและคืนคิวเดิม
- รองรับ: FR-BKG-02 (ASM-02, ASM-04)
- ตรวจด้วย: AC-BKG-02 (หลังบ้าน)
- ไฟล์ที่แตะ: `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/tests/test_AC_BKG_02.py`
- ต้องทำหลัง: T-05
- เสร็จเมื่อ: `test_AC_BKG_02` ผ่าน: ผู้รับบริการที่มีคิวที่ยังไม่ได้ใช้ในวันเดียวกันจองซ้ำไม่ได้ ไม่มีรายการจองใหม่ และได้ข้อมูลการจองเดิมกลับ
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 1: plan ข้อ 4 ยังไม่กำหนด response ของการปฏิเสธจองซ้ำ)

### T-07 เสนอ 3 ช่วงใกล้เคียงเมื่อช่วงที่เลือกเต็ม
- รองรับ: FR-BKG-03
- ตรวจด้วย: AC-BKG-03 (หลังบ้าน)
- ไฟล์ที่แตะ: `backend/app/slots/service.py`, `backend/app/booking/service.py`, `backend/app/booking/router.py`, `backend/tests/test_AC_BKG_03.py`
- ต้องทำหลัง: T-04, T-05
- เสร็จเมื่อ: `test_AC_BKG_03` ผ่าน: ช่วง 09.00 ถูกคนอื่นจองจนเต็มก่อน แล้วได้ 409 พร้อม 3 ช่วงว่างที่ใกล้ที่สุดในวันเดียวกันและวันถัดไป และจำนวนการจองของช่วงนั้นไม่เกิน capacity
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 2: ยังไม่ได้นิยามว่า "ใกล้ที่สุด" วัดอย่างไร)

## หลังบ้าน: การแจ้งเตือน

### T-08 วางงานส่งข้อความลงคิวโดยไม่รอผล
- รองรับ: IF-NOT-01, FR-BKG-04 (ส่งคำขอส่งข้อความ), FR-BKG-05
- ตรวจด้วย: AC-BKG-04 (ส่วน "การจองถูกบันทึก" และ "มีรายการในคิวส่งซ้ำ")
- ไฟล์ที่แตะ: `backend/app/notify/queue.py`, `backend/app/booking/service.py`, `backend/tests/conftest.py`, `backend/tests/test_AC_BKG_04.py`
- ต้องทำหลัง: T-05
- เสร็จเมื่อ: `test_AC_BKG_04` ผ่าน: เมื่อตัวส่งจำลองไม่ตอบสนอง POST /bookings ยังบันทึกการจอง และคิวในหน่วยความจำมีงานที่กำหนดส่งภายใน 5 นาที
- สถานะ: พร้อมทำ

### T-09 ส่งข้อความซ้ำตาม ASM-03
- รองรับ: FR-BKG-05, NFR-REL-02, IF-NOT-01
- ตรวจด้วย: AC-BKG-04 (ส่วน "กำหนดส่งภายใน 5 นาที")
- ไฟล์ที่แตะ: `backend/app/notify/queue.py`, `backend/tests/test_AC_BKG_04_retry.py`
- ต้องทำหลัง: T-08
- เสร็จเมื่อ: test ที่ใช้นาฬิกาจำลองยืนยันว่าส่งซ้ำไม่เกิน 3 ครั้ง ห่างกันครั้งละ 5 นาที และถ้ายังไม่สำเร็จจะบันทึกเป็นรายการค้างส่ง
- สถานะ: พร้อมทำ

### T-10 ต่อคิวส่งข้อความกับ Redis จริง
- รองรับ: IF-NOT-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของระบบจริงต่อจาก T-08 และ T-09 (test ยังใช้คิวจำลองตาม plan ข้อ 2)
- ไฟล์ที่แตะ: `backend/app/notify/queue.py`, `backend/app/config.py`, `backend/requirements.txt`
- ต้องทำหลัง: T-09
- เสร็จเมื่อ: ตั้งค่าให้ใช้ Redis แล้วจองได้ 1 รายการ และเห็นงานส่งข้อความในคิว Redis
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 3: `requirements.txt` ยังไม่มี library ของ Redis และ Codespace ยังไม่มีเครื่อง Redis)

## หลังบ้าน: การเข้าถึงข้อมูล

### T-11 สร้าง GET /bookings/{id}
- รองรับ: FR-BKG-05
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-13 (AC-BKG-06 เปิดดูข้อมูลการจองผ่าน endpoint นี้)
- ไฟล์ที่แตะ: `backend/app/booking/router.py`, `backend/tests/test_FR_BKG_05_get_booking.py`
- ต้องทำหลัง: T-05
- เสร็จเมื่อ: test ยืนยันว่า GET /bookings/{id} คืนรายละเอียดการจองและช่อง `queue_no` ได้ แม้การส่งข้อความจะไม่สำเร็จ
- สถานะ: พร้อมทำ

### T-12 ออกหมายเลขคิวเมื่อจองสำเร็จ
- รองรับ: FR-BKG-04
- ตรวจด้วย: AC-BKG-01 (ส่วน "แสดงหมายเลขคิว"), AC-BKG-02 (ส่วน "หมายเลขคิวเดิม"), AC-BKG-04 (ส่วน "แสดงหมายเลขคิว")
- ไฟล์ที่แตะ: `backend/app/booking/service.py`, `backend/tests/test_AC_BKG_01.py`
- ต้องทำหลัง: T-05
- เสร็จเมื่อ: `test_AC_BKG_01` ตรวจว่ามี `queue_no` ตามรูปแบบที่ได้คำตอบจาก Q-02
- สถานะ: รอ Q-02

### T-13 บันทึก audit log ทุกครั้งที่เข้าถึงข้อมูลการจอง
- รองรับ: DOM-PDPA-01
- ตรวจด้วย: AC-BKG-06
- ไฟล์ที่แตะ: `backend/app/audit/middleware.py`, `backend/app/main.py`, `backend/tests/test_AC_BKG_06.py`
- ต้องทำหลัง: T-03, T-11
- เสร็จเมื่อ: `test_AC_BKG_06` ผ่าน: เปิดดู GET /bookings/{id} 1 ครั้ง แล้วมีแถวใน audit_logs ที่มี actor_id, accessed_at และ hn
- สถานะ: พร้อมทำ

### T-14 ค้น HN จาก HIS ด้วยเลขบัตรโดยไม่เก็บเลขบัตร
- รองรับ: IF-HIS-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของการได้ hn ที่ใช้ใน T-05
- ไฟล์ที่แตะ: `backend/app/his/client.py`, `backend/app/main.py`, `backend/tests/test_IF_HIS_01_lookup.py`
- ต้องทำหลัง: T-02, T-03
- เสร็จเมื่อ: test ที่ใช้ HIS จำลองยืนยันว่า GET /patients/lookup คืน hn และไม่มีเลขบัตรประชาชนถูกบันทึกลงตารางใดเลย (วิธีเชื่อม HIS จริงดูข้อสังเกต 5)
- สถานะ: พร้อมทำ

## หลังบ้าน: ตรวจตาม Constraint และความเร็ว

### T-15 ตรวจ migration กับ PostgreSQL จริง
- รองรับ: CON-TECH-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-02 (test ปกติใช้ SQLite ตาม plan ข้อ 2)
- ไฟล์ที่แตะ: `backend/app/config.py`, `backend/app/db/migrations/001_init.py` (แก้เฉพาะถ้าพบจุดที่ใช้ไม่ได้บน PostgreSQL)
- ต้องทำหลัง: T-02
- เสร็จเมื่อ: ตั้ง `DATABASE_URL` เป็น PostgreSQL แล้ว `upgrade(engine)` สร้างครบ 3 ตารางโดยไม่มี error
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 4: จะใช้เครื่อง PostgreSQL จากไหน)

### T-16 ทดสอบความเร็วการค้นช่วงว่างแบบย่อส่วน
- รองรับ: NFR-PERF-01, FR-BKG-01
- ตรวจด้วย: AC-BKG-05
- ไฟล์ที่แตะ: `backend/tests/test_AC_BKG_05.py`
- ต้องทำหลัง: T-04
- เสร็จเมื่อ: `test_AC_BKG_05` ยิง GET /slots พร้อมกันแบบย่อส่วนตาม plan ข้อ 6 แล้วรายงานค่า p95 (ผลนี้ไม่ใช่หลักฐานว่าผ่าน AC-BKG-05 จริง ต้องวัดซ้ำบนเครื่องทดสอบ)
- สถานะ: พร้อมทำ

## หน้าจอ (ใช้ API จำลองตาม plan ข้อ 4)

### T-17 สร้างหน้าเลือกแพ็กเกจและช่วงเวลา (SlotPicker)
- รองรับ: FR-BKG-01, FR-BKG-06
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-18 (FR-BKG-06 ยังไม่มี AC ดูข้อสังเกต 6)
- ไฟล์ที่แตะ: `frontend/src/pages/SlotPicker.jsx`, `frontend/src/App.jsx`, `frontend/src/__tests__/FR-BKG-06.test.jsx`
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: test ที่ใช้ client จำลองยืนยันว่าหน้าจอแสดงช่วงเวลาพร้อมที่นั่งคงเหลือ และเรียก `getSlots` ใหม่ด้วย `package_code` ใหม่เมื่อเปลี่ยนแพ็กเกจ (รับรายการแพ็กเกจเป็น props ไปก่อน ดูข้อสังเกต 7)
- สถานะ: เสร็จ

### T-18 สร้างหน้ายืนยันและแจ้ง "ช่วงเวลาเต็ม" พร้อม 3 ตัวเลือก (ConfirmBooking)
- รองรับ: FR-BKG-03, FR-BKG-04
- ตรวจด้วย: AC-BKG-03 (หน้าจอ)
- ไฟล์ที่แตะ: `frontend/src/pages/ConfirmBooking.jsx`, `frontend/src/App.jsx`, `frontend/src/__tests__/AC-BKG-03.test.jsx`
- ต้องทำหลัง: T-17
- เสร็จเมื่อ: `AC-BKG-03.test.jsx` ผ่าน: client จำลองตอบ 409 พร้อม 3 ช่วง แล้วหน้าจอแสดง "ช่วงเวลาเต็ม" และปุ่ม 3 ตัวเลือก
- สถานะ: พร้อมทำ

### T-19 สร้างหน้าแสดงผลการจองและหมายเลขคิว (BookingResult)
- รองรับ: FR-BKG-04, FR-BKG-05
- ตรวจด้วย: AC-BKG-01 (หน้าจอ), AC-BKG-04 (หน้าจอ)
- ไฟล์ที่แตะ: `frontend/src/pages/BookingResult.jsx`, `frontend/src/App.jsx`, `frontend/src/__tests__/AC-BKG-01.test.jsx`, `frontend/src/__tests__/AC-BKG-04.test.jsx`
- ต้องทำหลัง: T-18
- เสร็จเมื่อ: test ทั้ง 2 ไฟล์ผ่าน: หน้าจอแสดง `queue_no` ที่ได้จาก client จำลองตามที่ได้รับมา โดยไม่ขึ้นกับผลการส่งข้อความ (ค่าจำลองต้องไม่บอกเป็นนัยว่าเลขคิวมีรูปแบบใด เพราะ Q-02 ยังไม่ได้คำตอบ)
- สถานะ: พร้อมทำ

### T-20 แสดงผลเมื่อถูกปฏิเสธเพราะจองซ้ำ พร้อมหมายเลขคิวเดิม
- รองรับ: FR-BKG-02
- ตรวจด้วย: AC-BKG-02 (หน้าจอ)
- ไฟล์ที่แตะ: `frontend/src/pages/ConfirmBooking.jsx`, `frontend/src/__tests__/AC-BKG-02.test.jsx`
- ต้องทำหลัง: T-18
- เสร็จเมื่อ: `AC-BKG-02.test.jsx` ผ่าน: client จำลองตอบว่าจองซ้ำ แล้วหน้าจอแจ้งว่าถูกปฏิเสธและแสดงหมายเลขคิวเดิม
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 1: ยังไม่มีสัญญา response ให้ client จำลองใช้)

### T-21 ต่อหน้าจอกับ API จริง
- รองรับ: FR-BKG-01, FR-BKG-02, FR-BKG-03, FR-BKG-04, FR-BKG-05, FR-BKG-06
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานรวมของ T-17 ถึง T-20 กับ T-04 ถึง T-07 (ตรวจซ้ำด้วยมือตามขั้น Given/When/Then ของ AC-BKG-01 ถึง AC-BKG-03)
- ไฟล์ที่แตะ: `frontend/src/api/client.js`, `frontend/src/App.jsx`
- ต้องทำหลัง: T-04, T-05, T-06, T-07, T-11, T-17, T-18, T-19, T-20
- เสร็จเมื่อ: เปิด `uvicorn` ที่ port 8000 และ `npm run dev` แล้วเลือกช่วงเวลา ยืนยัน และเห็นผลการจองจากหลังบ้านจริงบนหน้าจอได้
- สถานะ: รอทีมตัดสินใจ (ข้อสังเกต 1 และ 2 ผ่าน T-06, T-07, T-20)

---

## ตารางตรวจความครบ

### 1. AC ทุกตัวมี task ตรวจ
| AC ID | task ที่ตรวจ AC นี้ |
|---|---|
| AC-BKG-01 | T-05 (บันทึก, ที่นั่งเป็น 0), T-12 (หมายเลขคิว, รอ Q-02), T-19 (หน้าจอ) |
| AC-BKG-02 | T-06 (หลังบ้าน), T-12 (หมายเลขคิวเดิม), T-20 (หน้าจอ) |
| AC-BKG-03 | T-07 (หลังบ้าน), T-18 (หน้าจอ) |
| AC-BKG-04 | T-08 (บันทึก + เข้าคิว), T-09 (ส่งซ้ำภายใน 5 นาที), T-12 (หมายเลขคิว), T-19 (หน้าจอ) |
| AC-BKG-05 | T-16 (ย่อส่วน ผลจริงต้องวัดบนเครื่องทดสอบ) |
| AC-BKG-06 | T-13 |

### 2. Constraint ทุกตัวมี task ทำให้เป็นจริง
| Constraint ID | task ที่ทำให้เป็นจริง |
|---|---|
| CON-TECH-01 | T-01, T-02, T-15 |
| DOM-PDPA-01 | T-02, T-13 |
| IF-IDP-01 | T-03 |
| IF-HIS-01 | T-02, T-14 |
| IF-NOT-01 | T-08, T-09, T-10 |

---

## สิ่งที่ยังไม่ทำ

### Open Questions จาก spec.md
| ข้อ | รายละเอียด | task ที่รอ |
|---|---|---|
| Q-01 | ตอบแล้ว: "ช่วงเวลาใกล้เคียง" รวมวันถัดไป 1 วัน (พยาบาลคัดกรอง 2569-09-16) ย้ายไปอยู่ใน FR-BKG-03 | ไม่มี (ใช้ใน T-07) |
| Q-02 | หมายเลขคิวรีเซ็ตรายวัน หรือนับต่อเนื่อง และมีรูปแบบอย่างไร (เช่น A001)? -> ถามเจ้าหน้าที่เวชระเบียน (ยังไม่ได้คำตอบ) | T-12 และส่วน "หมายเลขคิว" ของ AC-BKG-01, AC-BKG-02, AC-BKG-04 |

### ข้อสังเกตที่ทีมต้องตัดสินใจ (ยังไม่มี ID ใน spec ถ้าจะแก้ ให้ทีมเพิ่ม Q หรือแก้ spec/plan เอง)
1. **response ของการปฏิเสธจองซ้ำ (FR-BKG-02):** plan ข้อ 4 กำหนดให้ POST /bookings คืน "booking id, queue_no หรือ 409 พร้อมช่วงใกล้เคียง" แต่ไม่ได้บอกว่ากรณีจองซ้ำตอบด้วยสถานะอะไรและมีข้อมูลอะไร ถ้าใช้ 409 เหมือนกรณีเต็ม หน้าจอจะแยกสองกรณีไม่ได้ ทีมต้องเติมสัญญานี้ลง plan ข้อ 4 ก่อน -> T-06, T-20, T-21
2. **นิยาม "ใกล้ที่สุด" (FR-BKG-03):** ยังไม่ชัดว่าจะวัดจากวันเวลาจริง (ช่วงวันเดียวกันมักใกล้กว่าวันถัดไปเสมอ) หรือจากเวลาในวัน (09.00 ของวันถัดไปนับว่าห่าง 0) ถ้าระยะเท่ากันจะเลือกช่วงไหน ต้องเป็นแพ็กเกจเดียวกันหรือไม่ และถ้าช่วงว่างมีไม่ถึง 3 ช่วงจะทำอย่างไร (AC-BKG-03 ครอบเฉพาะกรณีที่มีอย่างน้อย 3 ช่วง) -> T-07, T-21
3. **Redis:** plan ข้อ 2 เลือก Redis และบอกว่า "library ทั้งหมดอยู่ใน requirements.txt" แต่ในไฟล์ยังไม่มี library ของ Redis และ Codespace ไม่มีเครื่อง Redis รันอยู่ -> T-10
4. **เครื่อง PostgreSQL สำหรับตรวจ CON-TECH-01:** test ทุกตัวใช้ SQLite ยังไม่มีขั้นไหนยืนยันว่าตารางใช้กับ PostgreSQL ได้ ทีมต้องเลือกว่าจะรันจากที่ไหน -> T-15
5. **วิธีเชื่อมระบบยืนยันตัวตนและ HIS จริง (IF-IDP-01, IF-HIS-01):** spec และ plan ไม่ได้ระบุรูปแบบการเชื่อมต่อ T-03 และ T-14 จึงสร้างจุดตรวจที่เปลี่ยนตัวเชื่อมได้ และใช้ตัวจำลองใน test เท่านั้น ยังไม่มี task เชื่อมระบบจริง
6. **ข้อที่ยังไม่มี AC หรือไม่มี task:** FR-BKG-06 ยังไม่มี AC (plan ข้อ 6 ก็ระบุไว้แล้ว) ส่วน NFR-SEC-01 (TLS 1.2) และ NFR-USE-01 (ทดสอบกับอาสาสมัคร 10 คน) ไม่มีใน plan และไม่มี AC จึงยังไม่ได้สร้าง task
7. **รายการแพ็กเกจ:** plan ข้อ 4 ไม่มี API ที่คืนรายการแพ็กเกจให้หน้า SlotPicker ใช้ T-17 จึงรับรายการแพ็กเกจเป็น props ไปก่อน
8. **ระยะเวลาเก็บ audit log 1 ปี (DOM-PDPA-01):** plan ไม่ได้ระบุวิธีเก็บให้ครบ 1 ปี T-13 ทำเฉพาะการบันทึก และไม่มีโค้ดที่ลบ log
